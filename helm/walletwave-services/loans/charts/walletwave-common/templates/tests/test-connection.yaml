apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "walletwave-common.fullname" . }}-test-connection"
  labels:
    {{- include "walletwave-common.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "walletwave-common.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
